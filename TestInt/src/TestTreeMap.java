import java.util.Iterator;
import java.util.Set;
import java.util.TreeMap;
import java.util.Collection;

public class TestTreeMap {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		/*
		TreeMap<String,String> map_collection = new TreeMap<String,String>();
		for (Iterator i = map_collection.iterator(); i.hasNext();)
		{
			String str_collection = (String)i.next();
			if(str_collection.indexOf("KPI/") != -1)
				map_collection.put("A", str_collection);
			else if(str_collection.indexOf("_Current") != -1)
				map_collection.put("B", str_collection);
			else if(str_collection.indexOf("_Daily") != -1)
				map_collection.put("C", str_collection);
			else if(str_collection.indexOf("_Weekly") != -1)
				map_collection.put("D", str_collection);
			else if(str_collection.indexOf("_Monthly") != -1)
				map_collection.put("E", str_collection);
		}
		
		Set map_collection_keyset = map_collection.keySet();	//	{A,B,C,D,E}
		for (Iterator i = map_collection_keyset.iterator(); i.hasNext();)
			out.println(map_collection.get(i.next()));
			
			*/
	}

}
